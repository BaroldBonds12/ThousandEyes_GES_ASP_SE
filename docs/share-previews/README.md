# Share previews

HTML mockups for **Share with the group** workflow — how a project card will look on the hub before publish.

Generated by the `share-with-group` Cursor skill. Not deployed to the live site until the catalog entry merges.

Open locally:

```bash
cd docs/share-previews
python3 -m http.server 8080
```

Trigger in Cursor: **"Let's share with the group"**
